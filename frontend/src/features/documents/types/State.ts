import TDocument from './Document';

export type Document1State = {
  document1: TDocument;
  loadError?: string;
  loading?: boolean;
};

export type Document2State = {
  document2: TDocument;
  loadError?: string;
  loading?: boolean;
};
