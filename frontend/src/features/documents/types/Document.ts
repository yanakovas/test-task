type TDocument = {
  id: number;
  name: string;
  quantity: number;
  deliveryDate: string;
  price: number;
  currency: string;
};

// export type DocumentId = Document['id'];

export default TDocument;
